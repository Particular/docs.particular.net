> [!NOTE]
> ServiceControl Management is distributed as a portable application. It won't install on machines and it'll not add any start menu entry. To run the ServiceControl Management tool, relaunch the ServiceControl installer used originally to install ServiceControl.