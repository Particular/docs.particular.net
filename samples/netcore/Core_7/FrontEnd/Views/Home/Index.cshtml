@model FrontEnd.Models.CustomerModel

<div class="row">
    <form asp-controller="Home">
        <div class="form-group">
            <div class="card w-75">
                <div class="card-body">
                    <h5 class="card-title">Send a message</h5>
                    <p class="card-text">The following action will send a message through NServiceBus session.</p>
            
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control"/>
                    <span asp-validation-for="Name" class="text-danger"></span>
            
                    <input type="submit" value="Send Email" class="btn btn-primary mt-2"/>
                </div>
            </div>
        </div>

        @if (ViewBag.Message != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                @ViewBag.Message
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }
    </form>
</div>